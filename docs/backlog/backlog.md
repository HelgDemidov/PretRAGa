# Реестр открытой работы

**Это не спецификация.** Шаблону спецификаций не следует и в автоматические
обходы «что можно взять в работу» попадать не должен — исключается по имени
файла (`backlog.md`), а не по расположению.

Здесь то, что известно и не сделано по всему репозиторию. Три источника
пополнения: разборы карты истины, честный реестр дыр в
[../system_design/design_truth/README.md](../system_design/design_truth/README.md)
и то, что всплывает при работе. Строка удаляется, когда работа сделана, а не
помечается «готово» — правится по ратчету, объём не растёт монотонно.

Даты последней сверки здесь нет намеренно: рукописное поле «когда проверяли»
гниёт по построению, а историю правок и так знает git.

## Задачи

| # | Задача | Что сделать | Почему здесь |
|---|---|---|---|
| 1 | Разведочный код не имеет предохранителя | Заменить единственный переключатель «существует ли `src/`» на градацию: пока якорей ноль — сообщать, с первого якоря — требовать остальные. Добавить в карту объявленный блок разведочных префиксов; чекер их считает и печатает, представление показывает. Освобождение временное, названное и счётное | Сегодня `mkdir src` даёт 36 ошибок ещё до первой строки кода, а код вне `src/` не проверяется вообще — щель вместо предохранителя. Пока это так, работу над направлением зависимостей нельзя проверить на настоящем коде. ~40 строк, две функции по 31 и 11 строк |
| 2 | Направление зависимостей против Clean Architecture, портов нет | Ввести класс связи со смыслом «реализует порт»; один порт для исходящих вызовов закрывает 3 нарушения из 4; решить, чем `QueryLayer` пользуется у манифеста — механизмом или значением; поднять `foundation` наверх. Контракт import-linter генерировать ПОСЛЕДНИМ. Развилки и измерения: [ports_and_layers_brief.md](ports_and_layers_brief.md) | `foundation` внизу означает, что домену РАЗРЕШЕНО зависеть на инфраструктуру — правило CA обратное. Домен размазан по 6 рангам, отдельного слоя домена нет; карта не умеет выразить порт/адаптер ни породой, ни классом связи, ни пометкой. Цена измерена: подъём `foundation` наверх — 4 нарушения, полный разворот — 14 из 15. Поглощает прежний пункт про слабую нагрузку стека, включая пересмотр слоя `Reconciliation` |
| 3 | 20 незакрытых обязательств пород | Пройти «Реестр открытых обязательств» сверху вниз: на каждую позицию одно решение и одна правка карты — атрибут с `marks: identity` / `marks: version` либо связь `stored_in`. Начать с `Translation` | Решения владельца; реестр даёт готовый список работ, `--removal-impact` — цену |
| 4 | Конвенции остаются непроверяемой прозой, идентичность удвоена | Ввести сущностям поле `writer: machine \| human` и проверку «ровно один писатель». Конвенцию идентичности убрать из прозы `conventions`, оставив обязательством `identity` в `kinds` — одно правило в одном месте | Удвоение создано при переносе обязательств в породы и разъедется; правило одного писателя — несущее, а проверить его нечем |
| 5 | Проза несёт факты, которых нет в графе | Завести сущности для очереди склеек и уверенности извлечения — тогда «хранится в машинном хранилище» станет связью. «Якоря только в оригинале» оставить атрибутом `original_only` и записать как сознательно непредставимое ребром | Одиннадцать фактов подняты, три остались; расхождение прозы и графа не ловит ничто |
| 6 | 36 сущностей без якорей кода | Проставлять `implements` по мере написания кода: сущность получает якорь в том же коммите, что и её модуль. Порядок — от слоя `foundation` вверх | В день появления `src/` все 36 станут ошибками разом; массово заглушить больше нечем, и это правильно — но разложить надо заранее |
| 7 | Контракт слоёв import-linter не генерируется | Генерировать `[tool.importlinter]` из блока `layers` и якорей `implements` — когда якоря появятся | Ждёт якорей и разворота направления. Строить раньше — механизм без живого входа, то есть гипотеза, выданная за страховку |
| 8 | У атрибутов нет типа и кардинальности | Добавить поля `type` и `cardinality` из закрытых словарей одновременно с первой pydantic-моделью | Обещанный «дифф моделей» сегодня упирается в сравнение множеств имён; раньше моделей сверять не с чем |
| 9 | Имена пометок связаны с именами полей обязательств | Ничего, пока связка одна к одной. Развести имена и завести явное отображение — если появится пометка без парного обязательства | Наблюдение, не дефект: `marks: identity` и обязательство `identity` — одно понятие, названное один раз |

## Ожидают появления `src/`

Якоря, контракт слоёв и типы атрибутов блокированы не решением, а отсутствием кода.
До walking skeleton их нельзя ни сделать, ни осмысленно спланировать: сверять
карту не с чем.
